\documentclass[leqno, 12pt]{amsart}

\usepackage{seqsplit}
\usepackage[french]{babel}

\usepackage[utf8]{inputenc}
\usepackage{geometry}

\geometry{hmargin=2cm,vmargin=2cm}
\usepackage{lineno}

\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{amsthm}
\usepackage{mathtools}

\usepackage{graphicx}
\usepackage{subfig}
\usepackage{caption}
\usepackage{hyperref}
\usepackage{float}
\usepackage[table]{xcolor}
\usepackage{booktabs}

\usepackage{color}
\definecolor{darkblue}{rgb}{0,0,0.7} % darkblue color
\newcommand{\darkblue}{\color{darkblue}} % darkblue command
\newcommand{\defn}[1]{\textsl{\darkblue #1}} % emphasis of a definition


\title{Treillis join-distributifs}


\addto\captionsfrench{% Replace "english" with the language you use
  \renewcommand{\contentsname}%
    {Contenu du document}%
}

\numberwithin{equation}{section}
\newtheorem{thm}{Théorème}[section]
\newtheorem{lem}[thm]{Lemme}
\newtheorem{prop}[thm]{Proposition}
\newtheorem{defi}[thm]{Définition}
\newtheorem{cnj}[thm]{Conjecture}
\newtheorem{cor}[thm]{Corollaire}
\newtheorem{rmk}[thm]{Remarque}

\newcommand{\dps}{\displaystyle}
\renewcommand{\L}{\mathbf L}
\newcommand{\LS}{\downarrow \!}
\newcommand{\US}{\uparrow \!}
\newcommand{\Pos}{\mathcal P}
\newcommand{\Anti}{\operatorname{Anti}}
\newcommand{\MD}{\mathbf{MD}}
\newcommand{\Ss}{\mathfrak{S}}
\newcommand{\N}{\mathbb{N}}
\newcommand{\Z}{\mathbb{Z}}
\newcommand{\R}{\mathbb{R}}
\newcommand{\Irr}{\operatorname{Irr}}
\newcommand{\JIrr}{\mathcal J}
\newcommand{\MIrr}{\mathcal M}
\newcommand{\IdL}{\mathbf{Low}}
\newcommand{\Dis}{\operatorname{Dis}}
\newcommand{\Con}{\operatorname{Con}}
\newcommand{\Dec}{\operatorname{Dec}}
\newcommand{\Kappa}{\operatorname{K}}

\newcommand{\Part}{\operatorname{Part}}


\begin{document}
\maketitle

\tableofcontents

\section{Introduction}

Citer \cite{AFN22} \cite{Czed14} \cite{Arm09}

\section{Notations et préliminaires}


\begin{thm}
   Soit $\L$ un treillis fini. Les assertions suivantes sont équivalentes :
   \begin{enumerate}
   \item \label{item:1} Tout intervalle atomique de $\L$ est booléen ;
   \item \label{item:2} Tout élément de $\L$ s'écrit de manière unique comme le \emph{meet} d'une antichaîne d'éléments \emph{meet}-irréductibles ;
   \item \label{item:3} $\L$ est semimodulaire supérieurement et \emph{meet}-semidistributif ;
   \item \label{item:4} toutes les chaînes maximales de $\L$ ont pour longueur $|\MIrr(\L)|$ ;
   \item \label{item:5} $\L$ est un \emph{join}-sous-semitreillis d'un treillis distributif fini ;
   \item \label{item:6} $\L$ est isomorphe au treillis des fermés d'une clôture d'antiéchange (ou antimatroïde, ou géométrie convexe).
   \end{enumerate}
\end{thm}

Un treillis qui vérifie ces propriétés est dit \defn{\emph{join}-distributif}. Un treillis dont le dual est \defn{\emph{join}-distributif }est dit \emph{meet}-distributif.

Le nom "géométrie convexe" est motivé par le résultat suivant. Soit $E$ un sous-ensemble fini de $\R^n$. $A\subseteq E$ est dit convexe si l'intersection de $E$ et de l'enveloppe convexe de $A$ est égale à $A$. Les sous-ensembles de $E$ dont le complémentaire est convexe ordonnés par l'inclusion forment un treillis \emph{join}-distributif.

Un treillis \emph{join}-distributif étant \emph{meet}-semidistributif, il possède une surjection $\kappa:\JIrr(\Pos)\to \MIrr(\Pos)$ définie par $\kappa(j)=\max\{x\in \L:x\wedge j=j_*\}$.

%L'assertion (\ref{item:6}) est analogue au résultat suivant :

\begin{thm}
  Soit $\L$ un treillis fini. Les assertions suivantes sont équivalentes :
   \begin{enumerate}
   \item $\L$ est semimodulaire supérieur et atomique ;
   \item $\L$ est isomorphe au treillis des fermés d'une clôture d'échange (ou matroïde).
   \end{enumerate}
\end{thm}


\section{Premiers résultats}
\subsection{Définition de $\IdL(\Pos,\lambda)$}
\label{sec:def_low}
\begin{prop}
Soit $\Pos$ un poset fini, $n\in \N$ et $\lambda :\Pos \to \{1,...,n\}$ un étiquetage de $\Pos$.
$\IdL(\Pos,\lambda):=\{\lambda(X):X\in \IdL(\Pos)\}$ ordonné par l'inclusion est un treillis \emph{join}-distributif.
\end{prop}
\begin{proof}
  $\IdL(\Pos,\lambda)$ est stable par union donc c'est un \emph{join}-semitreillis, et il possède un plus grand élément $\lambda(\Pos)$, c'est donc un treillis~\cite[Proposition 3.3.1]{Stan12}. Les relations de couvertures de $\IdL(\Pos,\lambda)$ sont de la forme $\lambda(X)\lessdot \lambda(X)\cup\{k\}$, et comme le \emph{join} dans $\IdL(\Pos,\lambda)$ est l'union d'ensembles on en déduit que ses intervalles atomiques sont tous booléens.
  %Soit $X\in \IdL(\Pos)$ et $k_1,\dots,k_r$ tels que $\lambda(X)\cup\{k_1\}, \dots \lambda(X)\cup\{k_r\}$ couvrent $\lambda(X)$ dans $\IdL(\Pos,\lambda)$.
\end{proof}
Si $\lambda$ est injective, on a $\IdL(\Pos, \lambda)\cong \IdL(\Pos)$.
L'application $\lambda :\IdL(\Pos)\to\IdL(\Pos, \lambda)$ est un morphisme de \emph{join}-semitreillis, puisque $\lambda(X)\cup\lambda(Y)=\lambda (X\cup Y)$. On en déduit le résultat suivant :
\begin{prop}
   Pour tout $j\in \JIrr(\IdL(\Pos, \lambda))$, les éléments minimaux de $\lambda^{-1}(j)$ appartiennent à $\JIrr(\IdL(\Pos))$, \emph{i.e.} ce sont des idéaux principaux de $\Pos$.
   \label{prop:2}
\end{prop}
\begin{proof}
   Supposons que $x$ soit minimal dans $\lambda^{-1}(j)$ sans être \emph{join}-irréductible. Il existe donc $y$ et $z$ couverts par $x$ dans $\IdL(\Pos)$ tels que $\lambda(y)$ et $\lambda(z)$ sont différents de $j$. Or $j=\lambda(x)= \lambda(y\cup z)=\lambda(y)\cup \lambda(z)$, et comme $j$ est \emph{join}-irréductible, $\lambda(y)$ ou $\lambda(z)$ doit être égal à $j$.
\end{proof}

\subsection{Représentation des treillis \emph{join}-distributifs finis}
\label{sec:repr_join_dis}

$\kappa(j):=\max\{z\in \L:z\wedge j=j_*\}$ et $\gamma(j):=\{z\in \L:z\wedge j=j_*\}$

$\L$ est \emph{meet}-semidistributif si et seulement si $\kappa(j)$ contient un seul élément pour tout $j\in \JIrr(\L)$.
\begin{defi}
   Soit $\L$ un treillis fini. Pour tout $x\in \L$, on définit 
   \begin{equation}
        \Kappa(x):=\bigcup_{j\in \JIrr(\L),j\le x}\kappa(j),
   \end{equation}
   \begin{equation}
         \Gamma(x):=\bigcup_{j\in \JIrr(\L),j\le x}\gamma(j).
   \end{equation}
\end{defi}

$\Gamma(x):=\{m\in \MIrr(\L):x\nleq m\}$
\begin{prop}
Soit $\L$ un treillis fini. Pour tout $x\in \L$, on a
  \begin{align}
    \label{eq:lambda_mu1}
      x & = \bigwedge \{m\in \MIrr(\L)\setminus \Gamma(x)\} \\
      \label{eq:lambda_mu2}
      & = \min \big( \L\setminus \bigcup_{m\in \Gamma(x)} [\le m] \big)\\
      \label{eq:lambda_mu3}
      & = \min \big( \L\setminus \bigcup_{m\in \Kappa(x)} [\le m] \big).
  \end{align}
\end{prop}
\begin{proof}
L'égalité \eqref{eq:lambda_mu1} provient du fait que tout élément d'un treillis s'écrit comme le \emph{meet} des éléments \emph{meet}-irréductibles au dessus de lui. Pour tout $y\in \L$ tel que $x\nleq y$, il existe $m\in \MIrr(\L)$ tel que $y\le m$ et $x\nleq m$, ainsi $\L\setminus \bigcup_{m\in \Gamma(x)} [\le m]$ a un seul élément minimal qui est $x$, d'où \eqref{eq:lambda_mu2}. Pour démontrer \eqref{eq:lambda_mu3}, on montre que tout \emph{meet}-irréductible $m$ maximal dans $\Gamma(x)$ est dans $\Kappa(x)$. Par maximalité de $m$, on a $x\le m^*$. Comme $x\nleq m$, il existe $j\in \JIrr(\L)$ tel que $j\le x$ et $j\nleq m$. Soit $j$ minimal avec cette propriété, on a alors $j_*\le m$. Ainsi $m\wedge j=j_*$, et $m$ est maximal avec cette propriété, donc $m\in \Kappa(x)$.
\end{proof}

\begin{cor}
   Soit $\L$ un treillis fini. Pour tous $x,y\in \L$, on a 
   \begin{equation}
       x\le y \iff \Kappa(x)\subseteq \Kappa(y) \iff \Gamma(x) \subseteq \Gamma(y).
   \end{equation}
\end{cor}

\begin{prop}[\cite{HaNo18}, Théorème 2]
  Soit $\L$ un treillis fini. $\L$ est isomorphe à $\IdL(\JIrr(\L),\gamma)$.
\end{prop}

\subsection{Étiquetages minimaux}
\label{sec:min_label}
Dans la section \ref{sec:def_low}, on a vu que tout étiquetage d'un poset fini donne un treillis \emph{join}-distributif avec au plus autant de \emph{join}-irréductibles qu'il y a d'éléments dans $\Pos$.
Dans la section \ref{sec:repr_join_dis}, on a montré que tout treillis \emph{join}-distributif $\L$ peut s'obtenir à partir d'un poset étiqueté avec autant d'éléments qu'il y a de \emph{join}-irréductibles dans $\L$. 

On veut maintenant déterminer à quel condition $\Pos\cong \JIrr(\IdL(\Pos,\lambda))$. Cela nécessite d'avoir une bijection entre les deux, donnée par $x\mapsto \lambda([\le x])$. On veut donc savoir quand est-ce que les $\lambda([\le x])$ sont tous distincts et \emph{join}-irréductibles dans $\IdL(\Pos, \lambda)$.

\begin{lem}
   \label{lem:comparable_elem}
   Soit $x<y$ dans $\Pos$ tel que $\lambda(x)=\lambda(y)$. Il existe $z<y$ tel que $\lambda([\le z])=\lambda([\le y])$, ou alors $\lambda([\le y])$ n'est pas \emph{join}-irréductible dans $\IdL(\Pos,\lambda)$.
\end{lem}
\begin{proof}
   Si $x<y$ dans $\Pos$ avec $\lambda(x)=\lambda(y)$, alors $\lambda([< y])=\lambda([\le y])$. D'après la Proposition \ref{prop:2}, il existe un idéal principal $[\le z]$ strictement inclus dans $[\le y]$ tel que $\lambda([\le z])=\lambda([\le y])$, ou alors $\lambda([\le y])$ ne serait pas \emph{join}-irréductible.
\end{proof}


\begin{prop}
  Soit $(\Pos,\lambda)$ un poset étiqueté fini. Les éléments \emph{join}-irréductibles de $\IdL(\Pos,\lambda)$ sont en bijection avec les éléments de $\Pos$ si et seulement si :
  \begin{equation}
  \label{eq:criterion}
    \forall x,y\in \Pos, [x\neq y\text{ et } \lambda(x)=\lambda(y)] \implies \lambda([\le x])\nsubseteq\lambda([\le y]).
  \end{equation}
\end{prop}
\begin{proof}
  On montre d'abord ($\implies $) par contraposée. Supposons qu'il existe $x\neq y$ tels que $\lambda(x)=\lambda(y)$ et $\lambda([\le x])\subseteq \lambda([\le y])$.
  Si $\lambda([\le x])=\lambda([\le y])$, l'application $x\mapsto \lambda([\le x])$ n'est pas injective.
  Si $\lambda([\le x])$ est strictement inclus dans $\lambda([\le y])$, il existe $k\in \lambda(\Pos)$ différent de $\lambda(x)$ et $\lambda(y)$ tel que $\lambda([\le y])\setminus\{k\}\lessdot \lambda([\le y])$ dans $\IdL(\Pos,\lambda)$. Dans ce cas là :
  \begin{itemize}
  \item[$\bullet$] soit il existe $z<y$ avec $\lambda(z)=\lambda(y)$ et alors $x\mapsto \lambda([\le x])$ n'est pas injectif par le Lemme~\ref{lem:comparable_elem} ;
  \item[$\bullet$] soit $\lambda([\le y])\setminus\{\lambda(y)\}\lessdot \lambda([\le y])$ dans $\IdL(\Pos,\lambda)$, ainsi $\lambda([\le y])$ n'est pas \emph{join}-irréductible.
  \end{itemize}
  
 On montre maintenant ($\Longleftarrow$) par l'absurde. On suppose que \eqref{eq:criterion} est vérifié mais qu'il existe $x\neq y$ tels que $\lambda([\le x])=\lambda([\le y])$, ou alors que $\lambda([\le x])$ n'est pas \emph{join}-irréductible. 
 \begin{itemize}
 \item[$\bullet$]   Si $\lambda([\le x])=\lambda([\le y])$, alors $\lambda(x)\neq \lambda(y)$ par \eqref{eq:criterion}. Il doit donc exister $z<y$ tel que $\lambda(z)=\lambda(x)$ et $\lambda([\le z])\subseteq\lambda([\le x])$, ce qui contredit \eqref{eq:criterion}.
 \item[$\bullet$] Si $\lambda([\le x])$ n'est pas \emph{join}-irréductible, il a au moins deux couvertures basses $\lambda([\le x])\setminus \{\lambda(x)\}$ et $\lambda([\le x])\setminus \{k\}$ avec $k\in \Pos$ différent de $\lambda(x)$. Soit $I\in \IdL(\Pos)$ tel que $\lambda(I)=\lambda([\le x])\setminus \{k\}$. Il existe $z\in I$ tel que $\lambda(z)=\lambda(x)$, et $\lambda([\le z])\subset \lambda(I) \subsetneq \lambda([\le x])$, ce qui contredit \eqref{eq:criterion}. \qedhere
 \end{itemize}
 
 \end{proof}
 
  La bijection entre $\Pos$ et $\JIrr(\IdL(\Pos,\lambda))$ donnée par $x\mapsto \lambda([\le x])$ n'est pas nécessairement un isomorphisme (\emph{cf.} Figure \ref{fig:counterexample1}).
\begin{cor}
  Soit $(\Pos,\lambda)$ un poset étiqueté fini. Les posets $\JIrr(\IdL(\Pos,\lambda))$ et $\Pos$ sont isomorphes si et seulement si :
    \begin{equation}
    \label{eq:criterion2}
      \forall x,y\in \Pos, [x\neq y \text{ et } \lambda([\le x])\subseteq\lambda([\le y]) ]\implies [x\le y \text{ et } \lambda(x)\neq \lambda(y)].
    \end{equation}
\end{cor}
\begin{proof}
   Comme $x\le y\implies \lambda([\le x])\subseteq \lambda([\le y])$, on a un isomorphisme entre $\JIrr(\IdL(\Pos,\lambda))$ et $\Pos$ si et seulement si le critère \ref{eq:criterion} est vérifié et $\lambda([\le x])\subseteq \lambda([\le y]) \implies x\le y$.
\end{proof}
  
  \begin{figure}[H]
  \centering
  \includegraphics[width=0.7\linewidth]{figures/counter_example1}
  \caption{Un poset étiqueté $(\Pos,\lambda)$ et le treillis $\IdL(\Pos,\lambda)$. Les éléments de $\Pos$ sont en bijection avec $\JIrr(\IdL(\Pos,\lambda))$ mais ces posets ne sont pas isomorphes.}
  \label{fig:counterexample1}
  \end{figure}
  

\subsection{Complétion}

\begin{prop}
   Soit $\Pos$ un poset fini. $\MD(P)$ est \emph{join}-distributif si et seulement si $(\min \{x:x\nleq m\})_{m\in \MIrr(P)}$ est une partition de $\JIrr(\Pos)$.
\end{prop}

\section{Nouvelle famille de treillis (nom ?)}
Englobe les treillis \emph{join}-distributifs, géométriques, Kreweras...

Lien avec les greedoïdes (voirpar ex. tree-pruning greedoid)

\begin{thm}[{\cite[Théorème 8.8.7]{BjZi92}}]
Un treillis fini $\L$ est semimodulaire supérieurement si et seulement si il est isomorphe au treillis des fermés d'un "interval greedoid".
\end{thm} 

Tous les greedoïdes ne donnent pas des treillis sympas, il faudrait peut-être trouver une définition plus restreinte ?

Soit $\Pos$ un poset fini, $n\in \N$ et $\lambda:\Pos \to \Part(\{1,\dots, n\})$.

A voir si cette def est pertinente :

\begin{thm}
   Soit $\L$ un treillis fini. Les assertions suivantes sont équivalentes :
\begin{enumerate}
\item   Pour tout $x,y,z\in \L$ tels que $y$ et $z$ couvrent $x$, l'intervalle $[y,y\vee z]$ est coatomique ;
\item Pour tout $j\in \JIrr(\L)$, $\kappa(j)=\gamma(j)$ ;
\item Pour tout $x\in \L$, $\Kappa(x)=\Gamma(x)$.
\end{enumerate}
\end{thm}

Appeler ça treillis \emph{join}-modulaire ? On aurait \emph{join}-modulaire + \emph{meet}-modulaire $\implies$ modulaire, mais la réciproque n'est pas vraie (contre-exemple de taille 9)

\section{Treillis \emph{join}-distributifs semi-infinis}

Treillis des multipartitions (Izumi Watanabe)

Étendre les résultats de représentation, complétion des treillis join-distributifs aux treillis semi-infinis.

Voir "An extension of Birkhoff's representation theorem to locally-finite distributive lattices" \cite{Worl26}

Voir aussi \cite[Section 3]{AGT03}

\bibliography{lattices}{}
\bibliographystyle{plain}

\end{document}